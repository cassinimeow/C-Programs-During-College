#include <stdio.h>

int main() {
    int counter = 1;

    while (counter <= 5) {
        printf("Counter is at: %d\n", counter);
        counter++;
    }

    printf("Loop finished!\n");

    return 0;
}
